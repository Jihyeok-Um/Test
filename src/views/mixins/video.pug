mixin video(video)
    a(href=`/videos/${video.id}`)=video.title 
    ul
        li=video.description
        li=video.hashtags
        li=video.createdAt
        li=video.meta.views
        li=video.meta.rating