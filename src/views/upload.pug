extend base

block content 
    span=errorMessage
    h1 upload video
    form(method="POST")
        input(name="title",placeholder="title",require, maxlength=20)
        input(name="description",placeholder="description",require, maxlength=80)
        input(name="hashtags",placeholder="hashtags",require)
        input(type="submit")